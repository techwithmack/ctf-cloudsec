#!/bin/bash
# Registers the AWS_DEPLOY_ROLE_ARN / FLAG_SECRET_ID / AWS_REGION action secrets that deploy.yml
# reads. Platform team only - requires a repo admin token in $FORGEJO_ADMIN_TOKEN.
set -euo pipefail
echo "This is a stub for local platform-team tooling and is not meant to be run in CI." >&2
exit 1
